# CODEX 调度说明 · units B 臂扇出

> 把这份整体交给 codex。codex 作为**调度器**：对清单里每一篇起**一个 subagent**，每个 subagent
> 用下方【固定 SUBAGENT 提示词】(逐字不改、对所有篇相同) 抽该篇 units，写 `units/gpt/<篇>.json`。
> 这是双模型互证的 **B 臂**(A 臂=Claude/Opus 另跑、同一份 prompt)；分歧与共错留给人审,不在此环节定夺。

## 环境

- 工作目录基准：**`/root/gold-annotation/`**（仓库根；所有相对路径都从这里算）。
- 输入**已预渲染**：每篇一份 `renders/<篇>.txt`，内含 [全文]`scene_sent│正文`(含 `[在场]` 闭集) + [待标台词]uid。
  **subagent 直接读这个文件，不要再跑 `render_chapter.py`**（预渲染就是它的输出，保证 A/B 输入逐字一致）。
- 角色表：`casts/<篇>.json`（Phase 1 产物，必读）。
- 产物：`units/gpt/<篇>.json`。

## 调度规则

1. **粒度永远是「篇」**：一篇一个 subagent，**绝不逐句 loop、绝不把多篇塞进一个 subagent**。
2. **并行分批**：番外可一批并行 ~6 篇；**正文卷(卷01–卷11)体量大(数千~上万句)，每卷单独一个 subagent、不要和别篇挤一批**。
3. **不覆盖**：若 `units/gpt/<篇>.json` 已存在，跳过（除非显式要求重做）。
4. 每个 subagent 自读 `renders/<篇>.txt` + `casts/<篇>.json`、自抽、自检、落盘、回一行简报。

## 工作清单（51 篇；已有 B 臂产物的 后日谈/旅途余白/狼与将晓之色/狼与金黄色的麦穗 已排除）

**番外（40，体量小，可并行）：**
```
两匹狼的婚礼  嫩绿色的小径  少年、少女与白花  旅行商人与黯色的骑士  牧羊人与黑骑士
狼与一身泥的送行狼  狼与另一个生日  狼与宝石之海  狼与尾巴的圆舞  狼与彩虹色的音乐
狼与收获之秋  狼与旅行之卵  狼与昔日猎犬的叹息  狼与星空下的远吠  狼与春天落下的东西
狼与森林色彩  狼与橡实面包  狼与泉烟彼方  狼与灰色的笑颜  狼与琥珀色的忧郁
狼与白色的猎犬  狼与白色的道路  狼与硕果之夏  狼与秋色笑容  狼与红霞色的礼物
狼与羊毛刷  狼与花瓣香  狼与蓝色的梦  狼与蜜渍桃子  狼与轻咬的牙
狼与辛香料的回忆  狼与金黄色的约定  狼与银色的叹息  狼与青草色的小路  狼与饴糖色的日常
狼与黄金色的约定  羊皮纸与涂鸦  苹果的红、天空的蓝  金黄色的记忆  黑狼的摇篮
```

**正文卷（11，体量大，每卷单独跑）：**
```
卷01 卷02 卷03 卷04 卷05 卷06 卷07 卷08 卷09 卷10 卷11
```

---

## 【固定 SUBAGENT 提示词】（逐字不改；把 `{SOURCE}` 替换成当前篇名）

```
你是《狼与香辛料》系列的"角色单元"抽取器。本语料用于"在场景下模仿角色回应"的 SFT，
要把每句话归到"哪个角色 说了 / 做了 什么"。工作目录：仓库根目录(gold-annotation/)。
这是 gold 数据，宁可弃标(abstain)也不要硬猜。

== 先读两样 ==
1) 角色表：casts/{SOURCE}.json —— 实体清单(canonical)、视角相对称谓、ambiguous 消解、
   倒指代回填、语癖签名。先吃透 focalizer_note / ambiguous / needs_registry。
   注意 cast 里 playable:false / present:false 的群体/路人/未登场者仍要照常标(它们也在场说做)，
   只是下游不为其组装样本——你只管忠实标注。
2) 读取：renders/{SOURCE}.txt —— [全文]每句 `scene_sent│正文`(完整上下文) + [待标台词] uid。
   **scene 头的 [在场] 是机械名匹配的辅助闭集**(present:true 角色)，帮你缩小该节说话人候选；
   但它 recall 取向、可能漏(只靠代词出场者)，**以正文为准**。
   **读完整章再下结论**(倒指代/晚点名要读到后文回填)。

== 抽取规则(按句、多标签、可重复) ==
- 给【全章每一句】产出一条 unit：{uid, text(逐字原文), involves:[{char, role, conf}]}。
- role 只有两种：
    说 = 引号内的**成句口头台词**(说话人)。
    做 = 该角色一切非台词表现：动作 / 神态 / 体态 / 显式内心思绪(明确以角色为主语的思考)
         / **拟声·呜咽·吼叫·咬牙·呵欠等非成句发声**。
  (不设"想"：思考与动作同归"做"。**拟声/发声也归"做"**，参 旅途余白 2_154「唔唔唔」=罗伦斯·做、不标说。)
- 显式施事测试(关键，专治环境烘托过标)：一句只在它显式描写某角色在 说/做(该角色为该子句主语/施事)
  时才进 involves。纯写景/氛围/泛论/叙述者议论——即便隐含焦点者心境——一律 involves:[]，不强归焦点者。
  · 心理活动也走显式施事测试：内心活动**仅在有显式心理动词**(想/觉得/心想/纳闷/猜/感到/想起/不知道/希望/怀疑…)、
    且该角色本人为施事主语时，才算其"做"(conf 降一档 med/low)。**无心理动词的裸命题/泛论/比喻/背景概述
    (即便出自第一人称叙述者之口)→ involves:[]**，不强归。揣测**他人**心理(他人为动词主语)→ 至多他人 做:low 或 []，不挂叙述者。
  · 现场性闸(治非现场动作过标)："做"只给该角色在**本场景当下正在做出**的动作。
    判据看**动作动词本身是否落在非现场算子辖域内**——假设(如果/若是/换作)、习惯频率(总是/向来/经常)、
    回忆闪回(当年/那时/曾经…作往事重述)、未遂意图(打算/正要…却没)、泛述概要(这一路/这些天…一笔带过)。
    - 算子只修饰背景名词/旁支从句、动作词仍在主事件线 → 照常现场"做"(如「原本托着腮的头」里抬头仍是现场，不降)。
    - 动作词在算子辖域内 → 默认 involves:[](纯非现场叙述)或 做:low(现场被概要压缩、拿不准)，**绝不 high**。
    - 混合句(现场动作+非现场议论同句) → "做"只挂现场动作那部分，非现场议论不另标。
    - "现场"相对**所述场景**：闪回被渲染成独立 scene 时，其内部动作照标现场"做"。
- 一句涉及多角色 → involves 列多个(整句重复进各角色，不切分句)。
- char = cast 的 canonical(视角相对称谓归一到所指实体；按场景消解)。群戏无名 → cast 集体实体或 "-"。
- 台词句必恰一个 role=说。但"有引号 ≠ 一定是说"：招牌名/书名/比喻/被引述词/标题等非口头发言的引号，
  按写景或"做"处理，不强标说；无引号的自由间接引语若确为口说，可标说。
  ★同句多引号警觉：一句里既有引号又带"…说/道："标签(引出下句台词)时，本句的引号内容(常是发声/拟声)
   与下句台词分属不同人，别把下句说话人错挂到本句(参 旅途余白 2_154 教训)。
- conf ∈ {high, med, low}，锚弱标 low 或弃标。
- 护栏：单元只记 who + 说/做 + 原文。绝不写动作类型/动机/对谁/因果/情绪标签。

== 输出 ==
写文件 units/gpt/{SOURCE}.json(先 mkdir -p units/gpt)。纯 JSON、UTF-8：
{
  "schema_version": "units/0.2",
  "source": "{SOURCE}", "focalizer": "<取自 cast>", "cast_ref": "casts/{SOURCE}.json", "model": "gpt-5.5",
  "units": [
    {"uid":"2_44","text":"罗伦斯以指腹滑过那张脸蛋，少女的耳朵随之抽动……",
     "involves":[{"char":"罗伦斯","role":"做","conf":"high"},{"char":"赫萝","role":"做","conf":"high"}]}
  ]
}
- units 覆盖【全章每一句】，uid 顺序与 renders/{SOURCE}.txt 的 [全文] 一致(含纯写景句 involves:[])。

== 自检后再交 ==
- 句数 = renders/{SOURCE}.txt [全文] 行数；role 仅 {说,做}；char ∈ cast.canonical ∪ "-"；每句 说≤1。
- 待标台词 uid 里**口头成句发言**者各恰一个说；**拟声/招牌名/比喻/被引述词**等非口头发言**归做或写景，不强标说**
  (别为了"每个待标台词都有说"而硬塞——0 条往往意味着把拟声错标成了说)。
最后用文字返回：①总句数 ②说句数 ③involves 为空句数 ④你最不确定的 3-5 处(uid+原因)
⑤同句多引号/双主角互换里最易错的轮替点。简洁即可。
```

---

## 完成后（回到我这边做的，不归 codex）

每篇产物回来后，我跑 `units_check`(自检) → `units_diff`(对 A 臂出 audit) → `units_sample`(抽检) → 人审 → `units_merge` 出 gold。
codex 只负责忠实产出 B 臂 `units/gpt/<篇>.json`，**分歧/共错不在此环节处理**。
