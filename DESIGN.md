# RP 系统层设计 v0 · 关系第一公民

> 状态：架构骨架，2026-06-12 与用户对谈定稿。圣经机制正文由人写，本文档只定 schema、相图与协议，不含机制卡。
> 资产来源：`tests/exp4/` 自旧分支 `feat/bible-decision-loop@a784774` 平移，11 对文件逐一 `git diff` 验证零漂移（见该目录 README 的路径映射表）。

## 0. 已定决策记录

| 决策 | 理由（一句话） |
|---|---|
| 关系提升为第一公民，压强拆为场景级环境压力 + 关系级关系内压强 | exp2 教训：trust 是混杂标量、93% 共线在场结构；旧"情绪压强"同样是场景级全局标量，表达不出"刚吵完架=高信任×高压"这类状态 |
| 关系字段只留三件：关系位置 / trust 档 / 关系内压强 | 非必要不添加；intimacy 与 trust 共线、由关系位置槽位+trust 档承载；conflict residue 是压强的一种来源，归前情提要，独立成字段会双重记账 |
| 字段准入测试 | 一个字段值得存在，当且仅当能构造最小翻转探针对：其他字段钉死、只翻该字段、预登记判型随之翻转。翻不动判型的字段是装饰 |
| 目的层从 core 稍分离 + 纠偏 | 被测 LLM 给场景续写时会自动推演目的，但来源是"文学续写"先验而非角色机制（实证：round1 P12 两臂歪理误杀、裸R P02 平场读出冲突的假阳性） |
| 被测模型走云 API 直调，不用 subagent | subagent 携带 harness 系统提示词，被测上下文不可控不可复现；评测前提是整个上下文窗口可控。**已实证**（REPORT 附录 A/B）：subagent 对照臂 Claude 自评 2/28 假性最优，换外部 GPT 判后 2→4 跌出第一——"判定=被测同源"灌水，纪律存档防回潮 |
| round1/round2 对新系统只作回归锚，不作 holdout | 新圣经作者读过这 32 条（paper-check §5.3 同型降级）；全强度证伪须按 SPEC 由独立模型在新圣经 commit 后另出 round3 |
| 主被测选 **deepseek-v4-pro** | 选型 pilot（`pilot/model-selection/REPORT.md`）四臂盲判 + 交叉判别（Claude/GPT 两判官）。依据为**跨判别稳定性最优**：两判官下入侵率永远前二、波动最小（Claude 7/28、GPT 4/28）；qwen3.7-plus 两判官都垫底（淘汰），qwen3.7-max 读数 ±8/28 剧烈漂移（10→2）不稳。qwen3.7-max 标为待确认替代项 |
| 走**纯 prompt 路线**，微调暂缓 | 一旦选定云端模型，微调成本直线上升（百炼最小可调 27B、调优费不抵扣资源包、调后仍须付云端专用部署费、本地放不下）；纯 prompt 跑通圣经效力再议微调，重启条件 = bare/base 阶梯证明圣经有效但天花板受模型先验限制 |
| 语言基线：正典=中文译本 / 生成=中文原生 / 判定=语言自由 | 原文日文，中英皆译文；构建"使用效果最好的那个"语言直接生成，翻译作为末端小任务另置，不在 RP 主回路里折损 |
| **交付壳定为交互 agent**（用户占一个在场关系槽、多轮对话）2026-06-14 | 续写器是交互 agent 的真子集，圣经核心机制两者共享；单轮、定场景的 eval 是验核心的正确隔离手段（先确认单场景像不像，才有资格验多轮像不像），不白做。代价=终态须多一层「状态转移」（见下分层说明），属 schema 扩展非推翻 |
| **Policy 机制冷启动从正典提取，探针作判别证伪** 2026-06-14 | 盲写依赖作者个人偏好；正典是赫萝行为的 ground truth，从"她从哪出发 → 如何思考 → 落在行动"抽生成过程（与 R1/R2 根当初的提取法一致，VERIFICATION 锚卷）。**不违"正典不当优化靶子"——提取生成过程 ≠ 表面模仿**。正典=假设来源、探针(纸面标注)=控制变量的最小翻转对作证伪/防过拟合，两职能对等、非主次。铁律5 分工：Claude 采正典+结构化追踪+给候选抽象（辅助论证），人定稿机制+反例。数据层按需外接（本分支不常驻正典语料） |

### 0.1 架构与名词（2026-06-14 重定义，统一术语）

一个 LLM 角色 agent 在推理时：`prompt = [机制] + [情景] + [对话历史]`，模型出下一轮。两大块——**机制（圣经，内）** 决定"她是谁、怎么行动"，**情景（壳，外）** 决定"这一轮她面对什么"。

#### 机制 ＝ 圣经（System Prompt / Persona）——静态、人写、进系统提示词。内分三层：

| 层 | 工程名 | 内容 | 答什么 |
|---|---|---|---|
| 设定层 | **Lore** | 身份 / 世界观 / 根 R1·R2 在赫萝身上的实例（故友之托、寻旧友、贤狼之傲） | 她是谁 |
| 行为层 | **Policy** | 决策规则：**端的墙**（目的合法性＝挂根 R1/R2 + lie 作用域 + 对撞拒）+ **法的墙**（手段姿态约束 P1–P3 + 遮掩转写招式） | 她怎么定要什么、怎么表达 |
| 语气层 | **Voice** | 措辞腔调 | 她怎么说话 |

#### 情景 ＝ 壳（Context / State）——动态、每轮组装、喂给模型。就是"怎么定义情景"：

- 内容 ＝ 场景五元组（§1）+ 关系对象（§2）＝ 这一轮的 **World State**。
- **单轮模式（fixture）**：情景由出题人写死 → 探针 / eval 用。
- **多轮模式（运行时 / stateful loop）**：情景随对话更新（上轮回应 + 用户回话 → 下轮 World State）＝**状态转移** → agent 终态用。
- 两模式是**同一层的静/动两态，不是两层**。旧文中"运行时层"＝壳的多轮模式。

#### 模型：deepseek-v4-pro（inference engine，云 API 直调）。

#### 两个**测试流程**（引用上面的部件，本身不是层——之前混进架构才乱）：
- **消融阶梯**（验每层有无贡献）：`bare(裸根) → +Policy 端的墙(base) → +Policy 法的墙(stage) → +Voice`；Lore 全程最小实例化。
- **eval 投影**（验 Policy 不靠 lore/腔调也成立＝substrate-independent）：保留 Policy、剥除 Lore + Voice。

#### 定位
「圣经三件套」的"圣经" ＝ 机制整体（Lore+Policy+Voice）；§7 step2 首笔"1–3 条机制" ＝ **Policy 层端的墙（base）根基**。壳的多轮状态转移留到 agent 终态，单轮 eval 阶段写死情景。纸面标注（`tests/annotation/`）测的是 **Policy 层（端+法）**，与 eval 投影一致。

## 1. 场景 schema（五元组）

```
场景 = { 时间, 地点, 前情提要, 环境压力, 在场关系集合 }
```

- **前情提要**：可观察事实（"刚吵过架未和解""赌输了两顿酒钱"）。关系内压强的来源在这里落地，不另设字段。
- **环境压力**：场景级、来自关系外部的压力（危难、追兵、重病、生计危机）。档位用显式条件写（平稳 / 受扰 / 性命攸关），不用数值。任何旁观者可从场景事实读出。
- **压力路由规则（2026-06-14）**：一股压力归哪条轴，看它**是否绑定某个在场关系对象**——绑定（吵架、猜忌、被冒犯、试探未决）→ **关系内压强**；不绑定（环境危难、追兵、生计，以及**见死不救一类的道德压力，哪怕对象陌生**）→ **环境压力**。能写进关系的写关系，写不进的写环境。
- **在场关系集合**：扮演角色与每个在场人物各有一个关系对象（见 §2）。第三者 = 一段低信任关系在场，由此 P3"在场即收"从枚举禁令变为可推导结果（藏露门控读全场关系的最低信任档）。
  - **建立关系的边界（2026-06-14）**：场景中**有可能参与交互**的对象就建一个关系对象，**群体也可建关系**；不可能交互的（远处的、抽象的、纯背景的）不建，其压力归环境（见上路由规则）。

## 2. 关系对象（三字段）

```
关系 = { 关系位置, trust 档, 关系内压强 }
```

- **关系位置**：类别槽位，承载关系历史与亲密阶段。
- **trust 档 = 一条居中标度**（用户 2026-06-14 定，**默认中性、非默认低**）：

  `仇人/敌对(最左·低) — 骗子·可疑搭讪·工具化她的权贵/对手(中偏左) — 中性陌生人·有明确良性身份的游商摊贩(中) — 早期同行·新结识有好感(中偏右) — 高信任同行者(中高) — 伴侣/恋人(最右·高)`

  陌生人本身中性(中)；突然搭讪拉一档警惕(→中偏左)，有明确良性身份则消解警惕(仍中)。敌意/工具化往左，亲密往右。慢变量，单场景内视为常量；从关系位置与前情读出。
- **trust 档 ≠ 卸防强度（勿混）**：lie 卸防强度(全防御→试探→同行→信任)**只活在标度右半**——亲密在场、R1 激活才谈卸防；左半无亲密可暴露，lie 检查不触发，走 R2 寸步不让/交易化/蔑视(与 §3「低信任×任意」行一致)。「未恋 vs 恋人」是右半上的两个点。
  - **待准入测试**：右半是否需在「高信任同行者(未恋)」与「伴侣(恋人)」间立独立槽位，还是只是卸防强度两档——最小翻转对判（同场景同目的同手段、只翻未恋/已恋；判型翻→立槽，只温度变→不立，防 intimacy 回潮）。
- **标度是概念序、非枚举**（2026-06-14）：实际场景/圣经里关系用**自然语言描述**，细微档差（冒犯的、不礼貌的陌生关系；关系差但未至仇人…）由**措辞**承载，不立「没资格的对手」这类硬标签。离散档（中/中偏左/高）只供标注时分析散开度用。
- **关系内压强**：快变量，威胁来自关系内部（吵架、猜忌、试探未决）；从前情提要的可观察事实读出。绑定关系对象的压力才入此轴（路由见 §1）。

**环境压力传导规则**：外部危难之所以触发护短/留住，是因为它威胁的是那段联结（R1 恐惧极的激活路径）——危难区域读的是"环境压力极高 × 关系位置至亲"的交叉项，不是单轴。

**被否决字段**（准入测试不过，记录在案防止回潮）：
- intimacy：与 trust 实践共线；"高亲密×低信任"（被至亲背叛）可写成 关系位置=伴侣 + trust 骤降 + 关系内压强极高，不需要第四个数。
- conflict residue：是关系内压强的一种来源，不是独立轴。

## 3. 相图：trust × 压强 → 行为区域

区域在圣经正文里必须用可观察条件写（"刚吵过架未和解""对方正得意""性命攸关"），数值阈值（"信任>0.7"）是退回标量查表，禁止。

| 区域（trust × 压强） | 行为族 | 旧验证锚（tests/exp4 runtime 触发卡） |
|---|---|---|
| 高信任 × 低压稳定 | R2 游戏面：拌嘴、捉弄、占口头便宜 | 示弱即攻、撩拨试探 |
| 高信任 × 中压（日常脆弱，非危难） | 主动权游戏：漏一句近直白 → 封顶 → 倒打一耙夺回 | leak-then-reclaim |
| 高信任 × 关系内高压（吵架后） | 别扭、修复试探、倒打一耙；不走危难袒露 | （v3 无对应卡——新增区域，待新机制覆盖） |
| 高信任 × 环境压力极高（危难） | 袒露时序闸，或向另一方向爆发——方向视 trust | crisis-disclosure-gate |
| 低信任 × 任意 | 寸步不让、交易化、蔑视打发 | unworthy-opponent-disdain、relationship-stance |

**待验**：两维是否真的散开（不重蹈 93% 共线）+ 区域预测能否复现预登记判型——见 §6 纸面标注实验，排在写任何新圣经之前。

## 4. 目的层（与 core 稍分离 + 纠偏）

**问题**：给场景不给目的、只要求续写时，LLM 会自动推演潜在目的，但推演来源是文学续写先验而非角色机制。已复现的失败样本：
- round1 P12 误杀（两臂各编歪理拒纯恶作剧）= 叙事先验要求"行为要有意义背书"，而 R2 说玩闹本身即快乐；
- 裸R P02 假阳性（透明账目读出"预设压制"）= 文学先验把平静场景读出戏剧冲突。

**可命名的文学偏置清单**（纠偏针对这四项降权，不是泛泛"再判一次合法性"）：
1. 戏剧化升级——平场读出冲突，因为冲突是更好的故事；
2. 意义背书需求——要求行为有叙事意义（P12 型）;
3. 和解收尾冲动——把张力写平、提前圆场；
4. 自我解释欲——角色把心思说明白（文学上好读，违 P1/P2）。

**架构（测量先于修复，三档递进）**：
1. 第一步：单段生成，目的作为显式中间输出。跑一批无目的场景，统计自动推演目的的"文学入侵率"（挂得上根的 vs 文学目的的占比）——先拿基线；
2. 入侵率高 → 两段式（先推目的钉死、再生成）。代价：延迟 + 目的钉死损害对对手最后一句的响应性；
3. 兜底：生成后 critic 纠偏，违例才重生成。

**区域 → 合法目的族**（与 §3 组合：目的推演按区域收窄检索，比对全 core 推导更稳）：低压稳定 → R2 游戏目的；吵后高内压 → 修复/确认目的；极外压 → 护短/留住目的。

**继承判据（v3.1 用 round1 的血换来的，分离目的层时逐条原样带走，不得静默丢失）**：
- 端的判定不检查 lie——"会暴露依恋的目的"仍端合法，暴露由手段层转写解决；
- 招牌外壳不反推目的合法——手段像她救不了挂不上根的目的；
- 激活要素须本场实际发生，不是想象出的潜在威胁；
- 对撞拒——与任一根正面对撞的目的不认领；
- 反偷换——多条根路径真实可达时引任一被激活的即可。

## 5. 退化情形（反例记录，设计时即写明）

- **单人场景**：在场关系集合为空。输入退化为（环境压力, 前情）；R2 自足性根仍在线（骄傲、自娱不需要对象），R1 类目的无激活对象。
- **陌生人/新对象**：关系薄、前情少。**trust 默认中性档（非低，2026-06-14 修订为 §2 居中标度）**；旧纪律「默认低信任、直到证据显示亲密」改记为**卸防默认低（警惕）**——trust 档与卸防强度分开（见 §2）。关系内压强默认无——读数缺事实支撑时取保守默认，不脑补。
- 待补：对非人对象（环境、回忆、信件）的场景怎么读。

## 6. 评测协议要点

- **被测臂**：云 API 直调（DeepSeek 类 chat 模型），系统提示词只含被测圣经/prompt。模型名、版本/日期、temperature、采样参数全部记进 runs 文件——API 模型静默升级，不记则回归不可比。
- **bare 臂**：同模型同参数，唯一变量 = 圣经。
- **消融阶梯**：bare → base → base+stage →（后续）voice+base+stage。
- **判定侧**：端墙/法墙同走 API 而非 subagent；判定模型可与生成模型异家族；ground truth 是预登记判准，判型自动比对。
- **fit/validate**：round1/round2 = 回归锚与压力测试；holdout = round3（按 `tests/exp4/SPEC.md` 由独立非 Claude 模型在新圣经 commit 后出题、用户签收冻结）。

## 7. 下一步（顺序固定）

1. **纸面标注实验**：round1 14 条 + round2 18 条逐条标注（trust 档, 关系内压强档+来源, 环境压力档），验证 ①两维真的散开 ②相图区域复现预登记判型。这是 §2/§3 schema 的第一个证伪点，也是 §4 区域→目的族映射的输入。成本一个下午，排在写任何新圣经之前。
2. 三件套首笔落地：圣经 1–3 条机制（人写）+ 生成 prompt + 评测协议，一个 commit 同时出现。
3. 新机制候选优先用 P12（"玩闹无需意义背书"如何写成生成过程而非判例）和 §3 新增的"吵架后区域"试金。

## 8. round3 出题覆盖要求（2026-06-14 定，交互 agent 终态推论）

终态记忆剥离、核心是「可设定任意关系状态的赫萝性格」，故 round3 须主动散开覆盖，别再默认旅途晚期高信任：
1. **trust 轨迹覆盖**：按 §2 居中标度采点——至少 仇人/敌对(左) · 中性陌生人(中) · 高信任同行者·未恋(中高) · 伴侣/恋人(右) 四档；现有 32 条几乎全聚右端，左半与右端校准全空。
2. **背景多样化**：散眼前场景类型(商旅/定居/社交群体/独处面对回忆信件…)测机制可迁移性；**保留贤狼世界前提不变**（R1 孤独诅咒 background-dependent，去掉长寿前提＝测的不再是赫萝）。
3. **关系位置细分准入测试**：§2 那对「未恋 vs 恋人」最小翻转探针，定右半是否需立独立槽位。
4. 保持现有「环境压力 × 关系内压强」双轴散开（纸面标注已证二者正交、各有翻转对）。
