# canon · 正典只读快照

机制冷启动的来源（DESIGN §0：正典=假设来源，从"她从哪出发→如何思考→落在行动"抽生成过程）。

## 内容

| 路径 | 内容 |
|---|---|
| `maintext/卷01–11.json` | 正文 11 卷，结构 = `scenes[].sents[].text`（scene 自带上下文窗口）。 |

## 来源与纪律

- **快照来源**：`feat/units-presence-playable-gold-qc@d4be8e0` 的 `maintext/`，2026-06-14 平移。
- **只读**：本目录是参考底料，不在本分支加工、不回写。需要 units（说话人切分）/番外/管线时去 units 分支按需取，不重建管线（AGENTS.md 分支定位）。
- **用法**：提取机制时引具体场景进 `bible/` 工作文件并标 `卷N/scene` 锚；不把整库当常驻依赖。
