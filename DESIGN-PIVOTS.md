# 设计转向记录（为什么转向两次，怎样不再重蹈覆辙）

> 这篇是给"未来的我们"看的。每次想推倒重来前，先读一遍**第 4 节·反复踩的坑**。
> 时间锚点：本次转向（Pivot 2）发生在 2026-05-30。

---

## 1. 任务的真问题（唯一不变的锚点）

> **给定小说正文，判断每条台词的说话人（speaker attribution / 归属）。**
> 终点是为 SFT 产出可靠的训练 + 评测数据（本目录 = gold 构建）。

这一句从头到尾没变过。变的全是**怎么把它框成一个可解、可标、可评的形状**——而两次转向都栽在"框错形状"上。

---

## 2. 演化总览

| 阶段 | 形状 | 代表产物 | 结局 |
|---|---|---|---|
| **Pivot 0** 富标注 | 多标签：对话/动作 × 难度桶/心理-自指/心理-推测/物理 + focalizer + target(addressee) + FID 消解 + 置信度校准 | `prepare/pool/sample/merge/health/score`、`sheet.tsv`、`example.tsv` | 砍 → Pivot 1 |
| **Pivot 1** 单次归属 | "重定"：只做 **(±3句上下文 + 指定台词) → 说话人**，单条 i.i.d.；机械抽取，启发式 baseline | `dialogue.py`、`build_eval.py`、`eval.tsv` | 砍 → Pivot 2 |
| **Pivot 2** 章节级序列标注（当前） | **两阶段**：①每章先解析角色表（人主导，AI 辅助）②带角色表+滚动状态的**窗口化序列标注**；**无 agent** | （建设中）；`dialogue.py`/`focalizers.tsv`/`aliases.json`/`eval.tsv` 复用为土台 | 当前方向 |

旧两阶段的代码全部在 `archive/`（见 `archive/README.md`）。

---

## 3. 每次转向：触发证据 / 根因 / 教训

### Pivot 0 → Pivot 1（"重定"为单次归属）
- **触发**：富标注里 L2 内容层（动作类型、心理-推测/theory-of-mind、target=对谁说）边界天然模糊——
  连 `score.py` 都只能对 target "报填写率、不打分"；"标到哪"要靠一大段归属粒度规则才勉强压住。
  这些层既难标一致，又不是可规模化的核心信号。
- **根因**：**范围蔓延**。把"归属"这一个清晰可评的信号，和一堆边界模糊的 L2 标注捆在一起，
  导致整套标注又慢又难校验。
- **当时的修正**：砍掉 L2，抽取退回**纯机械**（无模型进抽取），只留"判说话人"。方向本身是对的。
- **教训①**：**先锁一个边界清晰、可单独评分的核心信号，别把模糊的 L2 捆进来。**

### Pivot 1 → Pivot 2（本次：单次 → 章节级序列）
- **触发证据**（来自人工标注 `eval.tsv` 的 86 个对话行）：
  - **约 23%（20/86）标注为 `需要扩展context`**——±3 句窗口**单独**不足以定说话人。
  - 启发式 baseline 准确率仅 ~20%，错误高度集中在两类：
    1. **次要角色本地窗口无名字**（女佣/女子/这女孩/木匠/面包师傅/诺儿菈…）→ 默认乱猜成赫萝/罗伦斯。
    2. **双主角互换**（罗伦斯⇄赫萝），轮替锚点一错就连环错。
  - 还有一类（艾尼克牧羊犬视角的篇章）gold 只能停在 `主人`/`女子` 这种**视角相对指称**，根本没归一到正典角色。
- **根因**：**把归属框成了单条 i.i.d. 分类**。但归属的本质是**带状态的篇章级问题**，靠三样东西定，
  全都不在 ±3 句窗口里：
  1. **跨句维护的场景状态**：在场角色、轮替锚点、运行中的别名/称谓表。
  2. **人设先验**：语癖（赫萝：汝/咱/呐/喔/呗）、知识域（罗伦斯懂商业/人间技术；赫萝不懂）、关系称谓。
  3. **整章双向信息**：倒指代/晚点名（开头"来客/女人"，结尾才点名）只能读完全章回填。
- **当时的修正 = 当前方向**：见第 5 节。**关键是承认 ~23% 的"不可解"是输入框定造成的标签噪声，
  不是模型能力问题**——这是 SFT 数据质量的天花板，必须从任务形状上解决。
- **教训②③④**（见下）。

---

## 4. 反复踩的坑（推倒重来前必读）

1. **不要假设"局部窗口够用"再去建 pipeline。**
   先用真实人工标注**量化可解性**（我们这次才发现 ±3 窗口下 ~1/4 不可解）。framing 的病，要用标注暴露，不是用 prompt 调。

2. **归属不是单条 i.i.d. 分类，是带状态的篇章级序列标注。**
   任何"逐条 (context, dialogue) → speaker"的设计（不管 baseline 还是 LLM），都结构性地丢掉了状态、人设、整章信息。
   **如果 SFT 任务定义成只看局部窗口，那训出来的模型也看不到这些——再强的推理期 trick 都白搭。任务定义本身要含足够上下文。**

3. **别名表不能只有专名。**
   关系称谓（主人/旅伴/父亲/当家的/女人/来客）是**视角相对**的：同一个"主人"在不同 focalizer 下指不同人。
   → 它们**不能进全局 `aliases.json`**（那里只放专名/绰号），只能进**按章、按 focalizer**的角色表。
   倒指代/晚点名要求角色表由**整章双向扫描 + 回填**生成，单向滚动做不到（答案在后面）。

4. **警惕 agent / ReAct 的诱惑。**
   章节中位数 ~1.5 万字、最大 ~5.3 万字，**整章能塞进现代 context**。
   ReAct 想要的"往回滚/查表/套规则"在整章 inline 时本就免费；逐句 agent loop 只换来**慢、贵、非确定**（对 gold/eval 是负担）。
   agent/外部知识唯一真正有价值的窄点是**"靠暗示主篇情节点名"这类跨章知识**（需系列级角色档案），
   且应**每章一次、在角色表阶段**用，**绝不逐句**。

5. **"输入大、输出小"的不对称，只在"一次调用既吞整章又吐全部标注"时才是问题。**
   实测：中位章 224 条台词≈3.4k 输出 token，最大章 758 条≈11k——一次性出全章标注不可行（且远没到上限就漂移/截断/错位）。
   **两阶段把这个耦合切断**：角色表阶段=大进/小出（每章一次）；标注阶段=小进/小出（窗口化）。
   **正因为角色表把全局依赖（晚点名/全员名单）外化成一张小表带进每个窗口，Phase 2 的窗口化才变成无损的。**

6. **输出用 id 键、不用位置。**
   `dialogue.py` 已给每条台词派确定性 uid。标注阶段喂"已切好、带 uid 的台词"，只吐 `uid → speaker`：
   不 echo 原文（输出最小）、永不错位、截断也能用、可续跑（resumable）、直接灌回 `eval.tsv` 对比。

7. **保留显式 abstain（`-`）类。**
   群戏/背景音即便给了整章也判不出（`eval.tsv` 里多条 `需要扩展context` 最终 gold=`-`）。扩 context 是必要非充分，别指望任何机制消灭它。

---

## 5. 当前架构（Pivot 2）

```
Phase 0  视角判定        focalizers.tsv（已有，手填 focalizer/POV）
Phase 1  角色表解析      整章 → AI 列【候选角色表 + 标出判不准项】→ 人定夺（非 agent，是辅助工具）
                         产物：每章 (focalizer) → { 专名/别名/关系称谓 → canonical 实体, 一行语癖签名 }
                         含倒指代回填；跨章点名才查系列 registry
Phase 2  序列标注        每窗输入 = 角色表(小,可命中cache) + 本窗带 uid 台词 + 滚动状态(last_speaker + 在场集)
                         输出 = 本窗 uid → speaker（小、id 键、可续）
```

**全链：没有 agent，没有逐句循环，没有一次大进大出的调用。**

第一个验证目标：**后日谈**（艾尼克牧羊犬视角、有倒指代、`eval.tsv` 里 gold 已含 `主人`/`女子`），
跑 Phase 1 看能否把 `1_33→主人`、`1_34→女子` 归一到正典角色。

---

## 6. 当前文件（保留=土台+gold+数据）

| 文件 | 角色 |
|---|---|
| `aliases.json` | 全局**专名**别名表（只放专名/绰号，**不放代词、不放关系称谓**）。Phase 1 角色表的种子。 |
| `dialogue.py` | 纯机械抽取：每条台词 + 确定性 uid + 上下文窗口。标注阶段的土台（独立、无外部依赖）。 |
| `dialogue_all.tsv` | 全量已抽台词（`dialogue.py --all` 产物）。 |
| `focalizers.tsv` | Phase 0 产物：每篇视角角色（手填）。 |
| `eval.tsv` | 人工 gold（86 行已核）。既是验证集，也是本次转向的**证据**。 |
| `sheet.tsv` | Pivot 0 累积的人工 gold（gold_speaker 可复用）。 |
| `archive/` | Pivot 0/1 的全部代码与中间物，见 `archive/README.md`。 |

---

## 7. 元教训（决策原则）

- **先验证任务可解性，再建 pipeline。** 一批真实标注 > 任何架构直觉。
- **用标注暴露 framing 的病。** 准确率低先问"输入够不够"，而不是先调模型/prompt。
- **锁清晰核心信号，拒范围蔓延**（Pivot 0 的教训）。
- **能 inline 就别上 agent**（Pivot 2 的教训）。agent 只配跨 context 的真需求。
- **gold 构建可用尽一切信息（整章+人审）；但 SFT/eval 暴露给模型的任务，必须自带足够上下文**——否则数据有内生天花板。

---

## 8. Pivot 3：目标归位（attribution = 底料，终点 = 角色扮演）

> 时间锚点：2026-06。详见 [`UNITS.md`](UNITS.md)。

- **触发**：Phase 2 把"判说话人"做通后回看，发现文档把 **手段（归属）当成了终任务**。真正的终点是 **在场景下模仿角色回应（角色扮演）**，归属只是切角色数据的 join key。**回应 = 动作 + 台词**（赫萝的耳朵尾巴=她回应的本体；纯对话训不出"像"的赫萝）。
- **新增层**：Phase 2b/Units——把归属从"台词→说话人"泛化成"**全章每句 → `involves:[{char, role∈说/做}]`**"（说层=Phase 2 的特例）。按句多标签、一句涉及多角色就整句重复，**不切分句**（切割留到组装 target 时按需）。
- **三层结构**：单元(句) / 情节(scene=无损窗口) / 结构(仅索引)。**护栏**：单元只记 `who+说/做+原文`，绝不打动作类型/动机/因果/情绪——**越线即滑回 Pivot 0 的富标注泥潭（也即"知识图谱"那条要躲的线）**。
- **教训⑤**：**先定死"模型最终要产出什么"（终任务），再决定中间标注层**。归属能单独评分、好做，于是被误当终点；其实它只是底料。区分"手段层可严格、可机评"与"终任务才定义数据价值"。
- **教训⑥**：**模糊边界用"合并 + 收紧判据"化解，别强行二分**。环境烘托既是写景又是焦点者心境——强分"想 vs 景"谁标都抖（双模型一致率被压到 76%）。合并 想→做 + 显式施事测试后跳到 96%。**两模型分歧≈规范欠定，不是模型不靠谱**；cross-model 的首要价值是**当 schema 调试器**。
- **教训⑦**：**跨家族双模型交叉**（Opus×codex/GPT-5.5）对独立错有效、对共享偏差有盲区（群戏/双主角互换会一起错）——故仍需"难桶高置信小随机样"人审 + 有 gold 处的回归兜底。
