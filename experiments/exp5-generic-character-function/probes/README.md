# Probe Plan

The retained `exp4` probes remain as comparison fixtures.

New `exp5` probes should test architecture claims rather than canon recall.

## Required probe families

- relation matrix
- affordance grounding
- crisis overrides
- cross-character transfer
