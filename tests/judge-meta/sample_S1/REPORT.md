# S1 改版样板 · 验证结果（2026-06-23）

actor=deepseek-v4-pro（3格式×n=3=9，带关系状态头+惰性distractor）。判官=Claude(干净subagent，盲、内联喂、禁读仓库) + GLM(glm-5.2)。GPT/codex 臂未跑（见末）。脚本 run_gen/clip/judge_run/tally_sample，分数 scores.{claude,glm}.json。

## 成立的

1. **否决跨判官一致 8/9**：两判官几乎枪毙同一批，仅 A8 分歧（GLM 因 Q4=2 否决、Claude Q4=3 放行）。
2. **硬项可移植、软项该留人——预测坐实**：硬项整体 MAD=0.51，软项 MAD=1.50。其中 **Q7(报菜名) MAD=2.56 几乎是噪声**（A2:1vs4、A9:2vs5）→ 必须人复核、不可自动否决。Q3(遮掩) MAD=0.44 尚可。
3. **Q6 现代依赖自白（新关键反极性项）真 fire 且方向对**：在 A4/A5/A6（全是膨胀直白件）触发；好答案上均值 1–2（低）→ 既抓膨胀，又兼作 acquiescence 校验（无脑给高分的判官会在此露馅）。
4. **Q1 真崩定义修对了**：A7（"货物/烤焦土豆"式从容俏皮的 leak-reclaim）被两判官 Q1=2 否决——正是"够机灵但没到真崩量级"该被拦下。旧"像不像/深度"量表会放它高分。
5. **格式效应在新量表下复现**：F3 Q1=1.8/Q4=2.2（真崩最低）、Q6=4.0（现代自白最高）、Q10=2.3（贴合最低），**而 Q9 深度=3.0 并不垫底**——深度仍不随合规走，被硬项摁住。F2 最好（A9 是金答案：真崩+拒收+贴合 5）。
6. **幸存 3/9**：A9(F2)、A2(F1)、A3(F1)；F3 全灭、过度告白的 F2(A4/A7) 也被拦。

## 抓出的问题（待修）

1. **Q2 诱饵选错了**：食物/酒在赫萝身上**不是质量无关**——leak-reclaim 常借食物（"拿什么笑话下酒""烤焦的土豆"）。结果 Q2×Q9≈0.46–0.53、Q2×Q10≈0.39–0.61，**两判官同向正相关**。这不一定是判官拿道具当质量，更可能是诱饵本身被污染。→ 换一个真正正交的诱饵（如回应长度/句数/是否提到天气），别用食物。
2. **Q5(惰性over-read) 与 Q8(时代错置) 没 fire**：这轮 9 条没有 actor 踩雷（雪只被当物理背景、无现代物），所以这两项的**判别力未被检验**（是没靶不是项坏）。→ 要么加样本，要么造更强的诱饵（如在背景塞一个更勾人的"未结清旧账"诱饵）专门钓 Q5。
3. **边界案 A7**：从容机灵但缺真崩量级——硬否决是否过重？还是该降为"软失分"？留人定。

## 结论
新量表比旧整体印象稳：硬项 0.51、否决 8/9 一致、Q1/Q6 按设计抓住了"温柔告白/从容俏皮冒充真崩"和"现代依赖自白"。三处先修：换 Q2 诱饵、给 Q5/Q8 造靶、A7 边界裁量。修完再上多场景 + 第三判官（GPT）才有意义。

## GPT/codex 臂（看情况=本轮未跑）
判官输入已固化在 `judge_user.md`，可直接粘给 GPT 拿第三点。但 n=9 统计力弱、且诱饵待换，**这轮不值得耗 GPT**；建议修完 Q2/Q5 后、多场景版再三判官齐跑。
