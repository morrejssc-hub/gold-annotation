# 正文-SOP.md — 正文 11 卷 units 抽取作业规程

> 番外 45 篇主要是**正文的预演**。番外真正交付的不是 gold 番外，而是这份**被数字标定过的正文 SOP**（臂数 / 规则补丁 / 阈值）。正文开抽前先读本文 + [`UNITS.md`](UNITS.md)（schema 与验证策略）。

## 0. 一句话

正文 gold = **高价值、高一致子集**，不追求标全。分歧 / 低置信 / 不确定一律弃出 gold 取值之外——**用 recall 换精度**（番外 45 + 正文 11 卷，料足够挥霍）。

## 1. 核心转向（为什么不再标全）

终目标是 (场景上文 → 角色回应) 的 SFT 对，**不需要每句都对，需要一个高精度子集**。因此：

- **gold = 多臂一致 ∩ 高置信**，scope 到 playable 角色的 说/做。
- **分歧 = 免费的不确定性探测器**：互证的价值在于自动圈出"该弃"的区域，不必逐句裁决。
- 我担心的是**静默失败**（错得自信进训练集），不是漏标。一致过滤正是针对"分歧型错误"的精度闸。

## 2. 预演实证（番外 3 篇，2026-06-07）

挑 qwen×opus 顶格分歧 3 篇（尾巴圆舞 / 宝石之海 / 两匹狼婚礼）补 sonnet 第三臂做三方对照，结论：

1. **多数投票不安全**。三方合议显示"高分歧≠opus 差"：宝石之海是 qwen 离群（18 句被另两臂否决）、尾巴圆舞 opus 最居中。`A16` 实锤 **opus+sonnet 跨家族共错**（真值赫萝，两臂都判罗伦斯）——多数投票会把这条静默写进 gold。证据见 `units/blind/`（盲审样本 + `_key.json`）。
2. **分歧大头是规则欠定，非模型噪声**。盲审 38 说话人 + 16 做涉及，分歧归到 5 类：
   - **#1 施事/受事混标**（共谋型，改规则）：现规则"整句涉及多角色→全列"与"显式施事测试"自相矛盾，把"主 看到/提及 受"里的受事也拖进 `做`。
   - **#3 说标到无引号框架句**（共谋型，改规则+机检）：把"开口/回答的──/…说："这类本句无引号的框架句标了 `说`。
   - **#2 心理动词白名单脆**（制造分歧，过滤即丢）：难懂/或许/看起来 等态度词卡在白名单外。
   - **#4 隐晦连续同人**（硬核，只能丢/人审）：连续两句同一人、无归属线索，三臂各处掉线。印证 CLAUDE.md 铁律#1。
   - **#5 群体角色乱归**（cast 集体实体策略未落地）：无名群体在 随行村人 / 主祭 / `-` 间散开，且挑错群体。
3. **覆盖率充裕**。最难 3 篇（3190 句）实测：**说话人维度全票一致 92%**、台词句说话人全票一致 80%、involves 全字段全票一致 68%（被 #1 受事噪声拖低）、且全 high 置信 59%。最难篇尚且如此，全语料只会更高 → **弃分歧后 recall 仍足**。

## 3. 架构：proposer–verifier（取代"双生成臂取一致"）

机械提议器实测（`units_propose.py --measure`，3 篇）：说话人覆盖仅 **4–22%**、精度 ≤69%、**`tag` 级=0**（本语料不在台词句里放"X说道："，已核实非 bug）。→ **机械对"谁"是死路**，信号在话语状态不在表层。

据此定架构：

| 维度 | proposer | 说明 |
|---|---|---|
| **说存在性**（哪句是台词） | **机械**（`dialogue.py` 引号 span，~100%） | 免费、确定；机械锁死 #3 共谋过标 |
| **说话人**（台词归谁） | **廉价模型**（`phase2` 或批量 API 单臂） | 机械不可行；模型出候选 |
| 验证 | **思考 / agent 模型** | 逐候选对原文查：对/错/弃；只留 verified-high |

- **2 臂即够，且必须跨家族**：弃多数投票后第 3 臂无用（纯成本）；但两臂须真异源（opus+sonnet 同家族共错过）。
- **分层验证**：廉价验证器扫全部 → agent 模式只对它 flag 的不确定句 / 焦点角色轮次重验（agent 能主动翻"上一句引号谁说"，可啃部分 #4）。
- **共谋通道被切断**：验证器对着**原文**查、不是跟另一模型对答案，比双生成臂"互相幻觉"更稳。

## 4. 正文作业步骤

1. **角色表先行**：正文 focalizer 恒为罗伦斯、**按卷一张 cast**（已在 `casts/`）。正文 units 只做归属，不重建角色（番外教训①）。
2. **机械锚台词**：`dialogue.py --all maintext` 出引号 span = 说存在性闭集。无机械 span 的句**禁标说**。
3. **全文进、分段出**，**按 scene 边界切**（`doc.scenes` 现成）——每段输出是完整话语单元，说话人状态自然重置；**绝不在 scene 中间硬切**（否则 #4 在长卷里雪崩）。正文 3–8k 句/卷，分段更刚需（番外教训②）。
4. **说话人 proposer**：`phase2` / 批量 API 单臂出候选（先按 §6 量 phase2-as-proposer 的覆盖/精度定用哪个）。
5. **验证 + 一致过滤**：验证器逐候选判，scope 到 playable 角色；**gold = 全票/双票一致 ∩ 高置信**，其余进 `units/abstain/`（留痕，非丢失）。
6. **抽检定泄漏率**（必做，§5）。

## 5. 抽检 / 泄漏率闸（唯一的人工成本）

- 共谋只藏在**一致集**里 → 抽检样本必须从"被留下的共识句"抽（不是抽已丢的分歧句），按难度分层（互换 / 群戏 / 晚点名 / 同句多引号）。
- 复用现成机制：`units_check` 的「同句多引号」零模型预筛（`⚑` 行）+ `units/fix_<篇>.tsv` 共谋修正通道（见 UNITS.md §5.B）。
- **设泄漏率闸**：抽检估计的静默失败率 > 阈值 → 收紧一致门槛 / 加异源验证。把残余风险量成一个数，不靠感觉。

## 6. 规则补丁（正文开抽前落地）

只改"会导致共谋同错"的两条；只制造分歧的（#2/#4/#5）过滤即丢、不必改（除非抽检发现它们也共谋）：

- **#1 `做` = 只施事**：删 UNITS.md/SKILL 里"一句涉及多角色→整句重复进各角色"，改为"`做` 只给该子句的施事；被感知/被提及/做状语的角色不进 involves"。与护栏不冲突（不是加"受事"标，是不标受事）。预期把 involves 全字段一致从 68% 拉高。
- **#3 `说` 必含引号**：`units_check.py` 加硬卡——无内联引号的句禁标 `说`。机械扫存量即可清掉一批假分歧。

## 7. 下一步

- [ ] 量 **phase2-as-proposer**（同 `units_propose.py --measure` 框架）对全票一致集的覆盖/精度，与机械 4–22% 对比 → 定说话人 proposer。
- [ ] 落 #1 / #3 两条规则补丁 + `units_check` 硬卡，量化挽回多少 recall。
- [ ] 搭验证器（思考模型，分层；agent 只上不确定/焦点轮次），跑通 1 篇番外（有人审真值）量验证后精度/召回。
- [ ] 正文按卷铺开：`dialogue.py --all maintext` → proposer → 验证 → 一致过滤 → 抽检。

## 关联

- [`UNITS.md`](UNITS.md)：units schema、验证策略（§5 分歧 vs 共谋）、组装。本文是其**正文落地的作业层**。
- [`CLAUDE.md`](CLAUDE.md)：三条铁律（①带状态序列标注 ②称谓视角相对 ③不逐句 agent loop）。
- 工具：`dialogue.py`（台词 span）、`units_propose.py`（机械提议器 + measure）、`units_check.py` / `units_diff.py` / `units_merge.py`。
- 预演产物：`units/sonnet/`（第三臂）、`units/blind/`（盲审 + key）、`units/propose/`（机械提议）、`units/qs_*`/`units/so_*`（三方 diff）。
