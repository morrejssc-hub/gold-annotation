# UNITS.md — 角色单元层（角色扮演数据的抽取与组装）

> 由 `UNITS-TRIAL-PLAN.md`（临时试做方案）落实而来。试点章 **旅途余白** 已全链跑通。
> 上游：`casts/<篇>.json`（Phase 1）。本层产物 → 角色扮演 SFT 样本。

## 1. 定位（终点正名）

终点 = **在场景下模仿角色回应（角色扮演）**。"判断每条台词的说话人"（Phase 2 归属）**不是终任务，是切角色数据的底料**——它告诉我们每句话归谁，才能组装 (场景上文 → 该角色的回应) 成对样本。
回应 = **动作 + 台词**，不只是引号里那串字（赫萝的耳朵尾巴=她回应的本体）。

## 2. 三层结构（与护栏）

- **单元(unit)** = 按句：`(某角色 说了/做了 什么)`，每句可涉及多角色。**最底层、唯一要抽的层**。
- **情节(episode)** = 一组角色交互，相对独立 = **scene**。它天然就是"自带足够上下文的无损窗口"（= 角色扮演样本的上下文边界）。
- **结构(top)** = 情节之间，**只做索引（顺序 + 在场），绝不抽因果/关系/弧线**（抽语义关系 = 滑回知识图谱 = Pivot 0 的坑）。

> **护栏（越线即废）**：单元只记 `who + 说/做 + 原文 verbatim`。**绝不**写动作的类型 / 动机 / 对谁 / 因果 / 情绪标签。

## 3. units schema（`units/0.2`）

`units/<model>/<篇名>.json`：
```json
{
  "schema_version": "units/0.2",
  "source": "旅途余白", "focalizer": "罗伦斯", "cast_ref": "casts/旅途余白.json", "model": "opus-4.8",
  "units": [
    {"uid": "2_44", "text": "罗伦斯以指腹滑过那张脸蛋，少女的耳朵随之抽动…",
     "involves": [{"char":"罗伦斯","role":"做","conf":"high"},
                  {"char":"赫萝","role":"做","conf":"high"}]}
  ]
}
```
- **每句一条**（全章所有句，非只台词）；纯写景/氛围句 `involves: []`。
- `role ∈ {说, 做}`（**不设"想"**）：
  - **说** = 引号内口头台词（说话人）。
  - **做** = 角色一切非台词表现：动作 / 神态 / 体态 / **显式内心思绪**。
- **显式施事测试**：一句只在它**显式描写某角色在 说/做**（该角色为该子句主语/施事）时才进其 involves。纯写景/氛围/泛论/叙述者议论 → `[]`，**即便隐含焦点者心境（环境烘托）也不强归焦点者**。
- **心理活动也走显式施事测试**：焦点者/任何角色的内心活动，**仅在有显式心理活动动词**（想/觉得/心想/纳闷/猜/感到/想起/不知道/希望/怀疑…）、且该角色本人为其施事主语时，才算其 `做`（`conf` 降一档 med/low）。**无心理动词的裸命题/泛论/比喻/背景概述（即便出自第一人称叙述者之口）→ `[]`**，不强归。揣测**他人**心理（他人为心理动词主语）→ 至多他人 `做:low` 或 `[]`，不挂叙述者。
- **现场性闸（治非现场动作过标）**：`做` 只给该角色在**本场景当下正在做出**的动作。判据看**动作动词本身是否落在非现场算子辖域内**——假设（如果/若是/换作）、习惯频率（总是/向来/经常）、回忆闪回（当年/那时/曾经…作往事重述）、未遂意图（打算/正要…却没）、泛述概要（这一路/这些天…一笔带过）。
  - 算子只修饰**背景名词/旁支从句**、动作词仍在主事件线 → 照常现场 `做`（如「原本托着腮的头」里抬头仍是现场，**不降**）。
  - 动作词**在算子辖域内** → 默认 `[]`（纯非现场叙述）或 `做:low`（现场被概要压缩、拿不准），**绝不 high**。
  - **混合句**（现场动作＋非现场议论同句）→ `做` 只挂现场动作那部分，非现场议论不另标。
  - 「现场」相对**所述场景**：闪回被渲染成独立 scene 时，其内部动作照标现场 `做`。
  - 表层触发词只作**抽检 surfacing 信号**（顶到抽检单让人确认），**不做自动降档/弃标**——实测朴素词规则假阳性过半（句中别处的触发词常误杀现场动作）。
- **多角色整句重复**：一句涉及 N 角色就进 N 个 involves，**不切分句**（切割留到组装 target 时按需做）。
- **"有引号 ≠ 一定是说"**：招牌名/书名/比喻/引述词/标题等非口头发言的引号，归写景或做，不标说；无引号的自由间接引语若确为口说，可标说。
- `char` = cast canonical（视角相对称谓归一到所指实体；按 cast.ambiguous/present_scope 消解）或 `-`（无可归属/群戏无名）。
- **conf 低档 = 质量门控**：边缘/焦点者推断的「做」标 `conf:low`，gold 层照记，**抽 target 时默认丢弃**（`units_assemble`，`--keep-low` 关）。gold 合并时 conf 取两家较低档（保守）。
- **`playable` (cast 字段)**：群体/路人/背景（…老板们、议长、领路的祭司、马、狼群…）标 `playable:false`；连同 `present:false` 者，`units_assemble` 不为其组装角色扮演样本。缺省 = 可扮演。

## 4. 管线与工具

```
casts/<篇>.json ─┐
                 ├─► [抽取] 双模型各一遍 ─► units/<model>/<篇>.json
render_chapter ──┘     (Opus 4.8 + codex/GPT-5.5，同一份 prompt)
                              │
            ┌── units_check.py（结构不变量 + 说层 vs Phase2 回归）
            ├── units_diff.py （跨模型 (uid,char,role) 三元组分歧 → 审计清单）
            └── units_assemble.py（按句序组装角色流/回合/角色扮演样本，纯机械）
```
| 工具 | 作用 |
|---|---|
| `render_chapter.py` | 整章紧凑视图（全文 scene_sent + 待标 uid），抽取的输入。**scene 头内联 `[在场]`**（机械 alias 匹配 canonical+aka 的 present:true 角色 + focalizer；称谓/泛指排除，约束#2）——逐节闭集提示，仍单请求整章、不滚动。 |
| `units_prompt_<篇>.txt` | 抽取任务提示词（A/B 逐字相同，仅换产物目录/model）。 |
| `units_check.py` | 单份产物自检：覆盖、role 域、说≤1/句、char∈cast；**抽检预筛·同句多引号桶**（零模型、不依赖 phase2，捞共谋盲区高发句给人工抽检）；说层 vs `phase2/<篇>.json` 回归（顺手白捡，非支柱）。 |
| `units_diff.py` | 两模型产物比对，分歧分桶（说话人/说存在性/涉及角色/说做角色），出审计 TSV（人工填 `audit` 列后即 `_v2`）。 |
| `units_merge.py` | 双模型 → 一份 **gold**：一致取共识（conf 取低档），分歧按 `audit_<篇>_v2.tsv` 裁决（A/B/点名角色），**共谋盲区按 `fix_<篇>.tsv` 覆盖**（uid→修正 involves；audit 管分歧、fix 管共错），产 `units/gold/<篇>.json`。**无 AI**。 |
| `units_assemble.py` | `--char` 抽某角色流、切回合、`--samples` 渲染 (上文→回合) 样本。**target 侧默认丢 conf=low 的「做」**（`--keep-low` 关）；`playable:false`/`present:false` 角色不组装。**无 AI**。 |

## 5. 验证策略

两类信号，作用层次不同，**别混为一谈**：

### A. 抓「分歧」——便宜，但有天花板
1. **结构不变量**（免费，`units_check.py`）：覆盖全句、role∈{说,做}、说≤1/句、char∈cast∪`-`、abstain 有据。
2. **跨家族双模型分歧**（`units_diff.py`）：Opus × codex/GPT-5.5 各一遍，分歧即审计信号。第二模型须**势均力敌、不同家族**（弱模型/同模型重采样无意义）。
> **天花板**：分歧才是信号，**两家(或更多)一起错、彼此不分歧的「共谋盲区」无信号**。靠堆运行次数/再比一份产物，永远撞不穿所有模型共享的错。

### B. 抓「共谋盲区」——唯一靠谱、且能规模化的（**主柱**）
3. **人工抽检** = 分歧集 ∪ low/med-conf ∪ **难桶高置信小随机样**（互换 / 群戏 / 晚点名 / **同句多引号**）。
   - 这是**唯一**能撞穿"所有模型一起错"的机制；**不依赖任何外部产物、按章采样、便宜**，每章都能做。
   - **难桶零模型结构预筛已内置 `units_check`**：**「句内含引号 + 句尾/引号前带『…说/道：』标签」**或**同句多引号**的句子（机械台词键定易错，如 `2_154`）自动入抽检池（`⚑` 行），把待检句捞给人，而非全章人读。旅途余白实测仅 2 句（2_154、6_1），精准。
   - 抽检发现的共谋盲区 → 经 `units/fix_<篇>.tsv` 修正通道回写 gold（`units_merge` 应用）。

### 附带（不依赖、不作支柱）：说层回归
- 有 `phase2/<篇>.json` 的章，units 的 `说` 投影回 uid→speaker 可与之对一眼——**纯属"既然有就别浪费"的顺手白捡**。
- **不作质检支柱**：phase2 是过渡产物，units 说层本就是它的超集；铺开后新章只做 units、不再单独做 phase2，故此信号只存在于少数已做章，且本质是"又跑了一遍"——能偶然撞出共谋盲区（如 `2_154`，靠 phase2 键法不同的运气），但**不可依赖**。共谋盲区的兜底永远是 B.3 抽检。

**旅途余白 实测**：收紧前两模型三元组一致 76%；**合并想→做 + 显式施事测试后跳到 96%**（分歧 136→25 句），说话人分歧 0、说存在性 0。残余 25 句全是"涉及角色"边界（焦点者推断算不算做）——已定为 `做`/低 conf。证实"分歧≈规范欠定，非模型不靠谱"。

## 6. 组装为角色扮演样本

- **单元 → 角色流**：按 uid 顺序过滤 `involves` 含该角色的句子。**纯机械、零 AI**（`units_assemble.py`）。
- **样本**：input = scene 上文；target = 该角色本回合的 说+做（verbatim）。
- **v0 = 整句 target（容忍串话）**：多角色共享句（旅途余白：赫萝 32% / 罗伦斯 24%）会把另一角色的内容也带进 target。v0 先全机械跑通闭环，**若保真不足，再对共享句做定点分句切割**（那时 AI 只进 ¼ 句、只渲染 target，gold 层不动）。

## 7. 与 Phase 2 的关系

units 的 **说层 = Phase 2 的 speaker，泛化版**（Phase 2 = 台词子集的单标签说话人；units = 全句多标签 说/做）。**phase2 是过渡产物，不是质检支柱**：units 说层是其超集，铺开后新章只做 units、不再单独做 phase2。有 phase2 的少数旧章，对一眼说层属 §5「附带·顺手白捡」，**不浪费而已，不作依赖**。

**旅途余白 回归实测（一次性白捡，记录留档）**：gold 说层 vs `phase2/旅途余白.json` 初始 96%（11 处不一致）。逐条核后——
- **9 处 phase2 错、units 对** → **units→phase2 回灌**（见 phase2 `revisions`），回归升至 99%。（签名口吻「汝/雄性」、汉娜「先生」通报、6_129/6_130 互换、场景3 议长↔老板们边界）
- **1 处 units 错、phase2 对**（`2_154`「唔唔唔」机械台词=罗伦斯咬牙发声；units 两家都把 `赫萝 说` 错挂此句——**共谋盲区**）：这条恰好被撞出靠的是 phase2「按机械台词键定」与 units「自由多标」**键法不同**的运气，**不可复制**；同类盲区的兜底是 §5.B.3 抽检（同句多引号桶）。
- **1 处保留**（`1_3` units 弃权 `-`，phase2 给了合理正解 领路的祭司，不以弃权覆盖）。
> 教训：跨产物比对只抓**分歧**，抓不到**共谋**。phase2 回归是过渡期白捡，**别把质检压在它上**；共享盲区一律靠抽检。

## 8. 进展

- **旅途余白 试点完成**：units schema v0.2 定稿；双模型 96% 互证；`units_check`/`units_diff`/`units_assemble` 跑通；赫萝/罗伦斯 角色流机械组装成功（赫萝 92 回合 / 罗伦斯 141 回合）。
- **gold 固化**：`audit_旅途余白_v2.tsv`（25 分歧：A3/B15/点名7）经 `units_merge.py` 回写 → `units/gold/旅途余白.json`（585 句，一致 560 + 裁决 25），结构校验全过。target 侧默认滤 low-conf 做后，赫萝 87 回合（含 low 92）。
- **说层回归一次性白捡**：gold vs phase2 撞出 11 处，9 处 units 对已回灌 phase2，`2_154` 共谋盲区经 `fix` 通道修正（删误挂的赫萝说）→ 说层回归 100%（仅余 `1_3` units 故意弃权）。**结论：phase2 比对退役为非依赖项，质检主柱转人工抽检（同句多引号预筛 + fix 通道已落地）**（见 §5/§7）。
- `[在场]` 已上 `render_chapter`，在 后日谈/狼与星空下的远吠/牧羊人与黑骑士 上泛化验证（全名/群戏/职称命中）。
- token：单章 Opus ~91k、codex ~59k（收紧后）。

## 9. 下一步 / 未尽事项

- [x] **人工固化** 旅途余白 units gold：`audit_旅途余白_v2.tsv`（25 分歧）→ `units_merge.py` → `units/gold/旅途余白.json`。
- [x] **说层回归（一次性白捡）已结**：gold vs phase2 11 处不一致 → 9 处 units 对、回灌 phase2（升 99%）；`1_3` 保留；`2_154` = 共谋盲区(units 双错)，待 §5.B.3 落地。**此后不再依赖 phase2 比对**（见 §5/§7）。
- **质检主柱：人工抽检（§5.B.3）**——共谋盲区唯一兜底、且每章可做。
  - [x] 零模型结构预筛「同句多引号」待检句已内置 `units_check`（全章通用、不依赖 phase2）。旅途余白捞出 2_154、6_1。
  - [x] **共谋盲区修正通道** `units/fix_<篇>.tsv`（uid→修正 involves）已落 `units_merge`：audit 管「两模型分歧」、fix 管「两模型共错」。`2_154`（删 `赫萝说`）为首例，gold 已回写。
  - [ ] 铺开后每章按预筛池 + 难桶随机样做一轮人工抽检，发现的共谋盲区进 `fix`。
- [ ] **v0 SFT 样本格式定稿**：用 `units_assemble.py` 输出 jsonl（input=场景上文 / target=角色回合），先 dialogue+action 整句 target。
- [ ] **沉淀 skill**：`units-extract`（双模型扇出 + 自动 diff），仿 `phase2-attribution`。
- [ ] **铺开**：番外 44 篇 + 正文 11 卷（maintext 按卷、focalizer=罗伦斯）。
- [ ] **共享句 target 切割**（option a）：仅当 v0 保真不足时启用。
- [ ] **情节层索引**（顺序+在场），仅在需要跨场景长程一致性时做，**不抽语义关系**。
